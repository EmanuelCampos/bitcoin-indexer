# Generated by https://jlopp.github.io/bitcoin-core-config-generator/

# This config should be placed in following path:
# $HOME/Library/Application Support/Bitcoin/bitcoin.conf

# [chain]
# Run this node on its own independent test network. Equivalent to -chain=regtest
signet=1
rpcworkqueue=1000
txindex=1
# [rpc]
# Username and hashed password for JSON-RPC connections. The field <userpw> comes in the format: <USERNAME>:<SALT>$<HASH>. RPC clients connect using rpcuser=<USERNAME>/rpcpassword=<PASSWORD> arguments. You can generate this value at https://jlopp.github.io/bitcoin-core-rpc-auth-generator/. This option can be specified multiple times.
rpcauth=bitcoine:2be2f983f631ddc297489fa35b518d63$ee609cfdfd8a0d64033b983899ffd78b8e995ba39e75bd9fa3a86f9200525d0e
rpcuser=bitcoine/123

# [zeromq]
# Enable publishing of block hashes to <address>.
zmqpubhashblock=tcp://127.0.0.1:3600
# Enable publishing of transaction hashes to <address>.
zmqpubhashtx=tcp://127.0.0.1:3600
# Enable publishing of raw block hex to <address>.
zmqpubrawblock=tcp://127.0.0.1:3600
# Enable publishing of raw transaction hex to <address>.
zmqpubrawtx=tcp://127.0.0.1:3600
# Enable publish hash block and tx sequence in <address>.
zmqpubsequence=tcp://127.0.0.1:3600


# [Sections]
# Most options automatically apply to mainnet, testnet, and regtest networks.
# If you want to confine an option to just one network, you should add it in the relevant section.
# EXCEPTIONS: The options addnode, connect, port, bind, rpcport, rpcbind and wallet
# only apply to mainnet unless they appear in the appropriate section below.

# Options only for mainnet
[main]

# Options only for testnet
[test]

# Options only for regtest
[regtest]